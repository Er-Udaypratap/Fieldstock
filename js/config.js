/* Fieldstock — backend connection config */

// Change this if your Django server runs on a different host/port,
// or when you deploy the backend somewhere else.
const API_BASE_URL = "http://127.0.0.1:8000/api";
